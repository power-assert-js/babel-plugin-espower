'use strict';

assert(falsyStr);

assert(falsyStr, messageStr);

assert.equal(str, anotherStr);

assert.equal(
str,
anotherStr
);

assert.equal(str, anotherStr, messageStr);

assert.equal(
str,
anotherStr
);

assert.equal(
str,
yetAnotherStr
);
