'use strict';

assert.isNull(falsy);

assert.isNull(falsy, message);

assert.same(foo, bar);

assert.same(foo, bar, message);

assert.near(actualVal, expectedVal);

assert.near(actualVal, expectedVal, delta);

assert.near(actualVal, expectedVal, delta, message);
